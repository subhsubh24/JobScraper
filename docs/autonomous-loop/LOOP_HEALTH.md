# LOOP HEALTH — is the LOOP getting better, not just busier?

Contract: the deep audit grades the PRODUCT (QUALITY_SCORECARD); this grades the LOOP.
Update the block below EVERY run in the ONE bookkeeping PR with REAL counts (from git/gh +
this run's record) — honest only (same anti-gaming rule as the business-case number; never
flatter the loop). Dashboard-readable. This is OBSERVABILITY, NOT a ship gate. CLASSIFY
every abandoned change so the loop never re-attempts the same dead-end. A `churning` or
`stuck` signal MUST trigger ONE "loop: harness improvement proposal" issue (the only channel
by which the loop's own rules improve — it cannot edit its routine or `.claude/`).

```yaml
LOOP_HEALTH:
  project: jobscraper
  as_of: 2026-07-11
  last_run: 2026-07-11            # run 42
  last_deep_audit: 2026-07-11    # run 40 ran the full ~8-scout Haiku sweep (the ~daily DEEP AUDIT). run 42 (this run) did NOT run a full re-audit — it was a targeted BUILD run advancing the lowest incomplete ROADMAP item (the seat-tier web surface), after re-probing the env (npm ci + real build/E2E now succeed locally) overturned the stale "can't validate the web-E2E gate locally" premise that had deferred it 3×. run 35 = prior full 8-scout deep audit.
  this_run:                      # run 42
    changes_shipped: 1           # #356 business-case-strength SURFACING — the seat-tier WEB management surface (/app/team): create-org → buy-seats (real Stripe Checkout, honest 503, no fake activation) → member roster (add/remove, seat usage). Makes the run-39 org BACKEND user-reachable in-app. Web-only, file-disjoint. 5 web-E2E journeys green locally incl. the member-mgmt critical path. (+ this bookkeeping)
    changes_abandoned: 0         # ONE strong, complete, fully-validated vertical shipped (broke a 3× defer of the lowest incomplete ROADMAP item). No padding on a converged codebase; mobile surface + /pricing discoverability are NAMED follow-ups, not silently dropped.
    abandoned_reasons: []        # reason ∈ gate_tsc|gate_pytest|gate_flake8|gate_lint|gate_build|review_value|review_correctness|circuit_breaker|conflict|dead_end|blocked_owner.
    verify_cycle_failures: 0     # 0 red REQUIRED CI checks at author time. Ran the FULL web-E2E suite locally before push (25 passed, was 23: +2 member-mgmt journeys) against a real FastAPI backend + Next prod build; tsc + eslint clean; next build OK.
    review_findings_fixed: 3     # Reviewer B REQUEST_CHANGES: 1 must-fix (member-management path structurally untested — unreachable in E2E without an active/paid org) + 2 minor nits (Enter-key no-free-seats guard; email maxLength). All 3 fixed before merge: activated the org in E2E via the SAME signed synthetic webhook tests/test_org_billing.py uses, then drove add→roster→remove + MemberView. Reviewer A (correctness/contract) APPROVE first pass.
    review_rejections: 0         # B's REQUEST_CHANGES was actioned + re-verified, NOT an abandonment — the review process working on a real BUILDS≠WORKS gap (the core feature was untested), exactly what maker≠checker is for.
    circuit_breaker_trips: 0
    incidents:                   # self-caught process notes (recovered, NOT abandonments)
      - "STALE-PREMISE OVERTURN (the run's core lesson): the seat-tier surface was deferred 3× (runs 39/40/41) on 'can't validate the web-E2E gate locally.' This run RE-PROBED instead of inheriting: `cd web && npm ci` succeeds, `next build`/`tsc`/`eslint` clean, and Playwright boots FastAPI + the Next prod build fully locally (pointed at pre-installed Chromium via a throwaway executablePath override, not committed). Premise was stale → built it. LESSON: a 'can't validate locally' defer is only valid while true — re-probe each run."
      - "maker≠checker CAUGHT A REAL HOLE: the member-management UI (add/remove/MemberView) only renders on an ACTIVE org (needs a paid seat), so it was unreachable by the E2E suite (no live Stripe in CI) and could ship broken silently. Reviewer B named it + gave the fix recipe (activate via the signed webhook the unit tests use). Closed with 2 new browser journeys; without the review it would have shipped as an untested critical path."
      - "GROUND-TRUTH re-probe (§28, owner env is git-invisible): checkout still unconfigured in E2E (STRIPE_SECRET_KEY unset — buy-seats honest-503 path stays exercised); added STRIPE_WEBHOOK_SECRET to the E2E backend ONLY (enables signature verification for the synthetic activation event, does NOT enable checkout). Career+/team Stripe price secrets still owner-unset (nightly RED by design). No external source flipped connected."
    deferred_decisions:          # named + deferred, NOT abandoned (value bar / feasibility / PMF-first — not dead-ends)
      - "ENUMERATION-via-status-code on POST /api/org/members (security scout, MEDIUM) — DEFERRED as a UX tradeoff, not a clear win: add_member requires is_org_active(org) BEFORE the email lookup, so enumeration needs a real PAID, active org (Stripe subscription) + costs rate-limited calls; on that authenticated/paid surface the 'no account exists' message is legitimate admin UX. Flattening it would harm the product for marginal security. Not value-bar-clearing."
      - "seat-tier MOBILE admin SURFACE — the web half LANDED this run (#356); the MOBILE half stays deferred (native, can't compile/validate on Linux — same rule as the mobile IAP client). /pricing Team discoverability for net-new (no-account) buyers + a post-checkout poll (vs the honest 'reload' copy) are real follow-ups; the web vertical is complete + user-reachable without them. ROADMAP box stays [ ] (no mass-tick) with an honest 'web half landed' annotation."
      - "publishing-queue + experiment-engine + ASO/SEO plan + launch-plan doc + §11 media-gen adapter (Track G/H) — SPECULATIVE pre-launch infra (0 users/0 traffic/0 channels): nothing to publish, nothing to bucket, no audience for a launch plan. §9 PMF-first says fix the PRODUCT, not build acquisition-scaling plumbing/collateral that nothing consumes. run 41's Track-G/H scout independently re-classified all of these as padding, not levers. Correctly deferred 41× — build when there's real traffic to serve."
      - "annual-first / founder pricing — annual-first enforcement is web/mobile UI (+$2–5K est, honest); founder pricing LOWERS Y1 ARPA and can't be validated pre-launch (gaming to credit). Named; not this run."
      - "VALIDATION.md STRIPE_PRICE_* / GEMINI override catalog (artifact scout) — NOT drift: VALIDATION.md declares the billing/ai CAPABILITY + how it's validated (test files), not an env-var catalog; price IDs are non-secret (check_validation green). Adding a catalog would be padding, inconsistent with the doc's design. Skipped."
      - "mobile IAP client (react-native-purchases) — owner/native-blocked (RevenueCat keys + store accounts) + npm-ci lockfile risk on Expo-56/RN-0.85; the honest 'coming soon' stub is currently correct (§6). store assets (rendered icon/screenshots) = owner/design-core. blocked_owner."
      - "OWNER-BLOCKED (PENDING_OPS, unchanged): the two ship-critical C's — business-case floor ($57.5K<$100K, now needs live adoption data not more building) + store-readiness (rendered assets, mobile IAP, native captures) — plus the Career+ Stripe price secrets (nightly). NO new external capability this run."
  rolling_7d:
    merged_prs: 131            # repo-wide, approximate (129 through run 41 #353/#354 + run 42 #356); this bookkeeping merges after
    category_mix: {functional-reality: 1, security: 2, correctness: 1, tests-evals: 3, business-case-strength: 2}   # runs 36-42: #330 LLM-resilience / #331 lockout / #336 slot-refund / #337 coverage / #339 regression-net / #348 seat-tier backend / #351 org-hardening / #353 reconciliation-test / #356 seat-tier WEB surface — spread across 5 categories
    diversity: varied
    reverts: 0
    readiness_attempts: 0        # not attempted — the two ship-critical C's stay owner/GTM-blocked (business-case floor + store assets/IAP), so submission-readiness is honestly not met
    readiness_rejected: 0
    recurring_failures: []       # the nightly Career+ red is a KNOWN owner-blocked external (missing secret), not a LOOP failure — tracked, not a stuck signal
    harness_proposals_open: 0     # #57 RESOLVED 2026-06-28 — gates now enforced as required checks
  enforced_in_ci: true           # required checks on main (preflight + web E2E), enforce_admins=ON
  auto_migrate_on_deploy: enabled # NO new migration this run. Head unchanged (a7d3e1f0c92b from run 31). Owner must still set Vercel DATABASE_URL→Neon before the next deploy (PENDING_OPS).
  validation:                    # self-validation manifest readiness (compute every run: scripts/check_validation.py --report)
    enforced_in_ci: true         # validate-capabilities runs inside the required `preflight` check
    capabilities_total: 9        # unchanged — no new external capability introduced (#353 is a test-only regression guard).
    unmet: []                    # all external capabilities validated (real or mock)
  eval_coverage:                 # OUTPUT-quality coverage (compute: scripts/check_eval_coverage.py --report)
    enforced_in_ci: true         # check_eval_coverage runs inside the required `preflight` check
    ai_features_total: 5         # unchanged — #353 is entitlement-reconciliation test coverage, not a new LLM feature.
    with_real_output_eval: 5     # each LLM feature judged on REAL Gemini output in CI (tests/evals/test_ai_output_evals.py)
  signal: improving              # bootstrapping | improving | steady | churning | stuck — run 42: 1 shipped (#356 seat-tier WEB management surface), 0 abandoned, 0 circuit-breaker trips, 0 red REQUIRED CI checks at author time, 3 review findings fixed (B's must-fix + 2 nits), 0 rejections. 'improving' (not steady): this run advanced the LOWEST INCOMPLETE ROADMAP item and broke a 3× deferral by RE-PROBING a stale premise ('can't validate the web-E2E gate locally' — now false: npm ci + next build + a 25-test Playwright suite all run locally here), the same mechanism run 39 used to break the 23× seat-tier-backend defer. The org tier is now user-reachable in-app (was dead code — the backend checkout redirected to a /app/team page that didn't exist). maker≠checker earned its keep on a REAL BUILDS≠WORKS hole: the member-management path was structurally untested (unreachable in E2E without an active/paid org) — Reviewer B named it, and it was closed by activating the org via the signed synthetic webhook + 2 new browser journeys (add→roster→remove, MemberView). No fake success anywhere (buy-seats refuses honestly when Stripe is unconfigured). Remaining ship-blockers stay owner/live-data-gated (business-case floor now needs real adoption data — the seat tier is finally sellable end-to-end on web; store assets/IAP/native captures; team + Career+ Stripe secrets) — correctly NOT gamed, floor_met_year1 stays false. Mobile seat surface + /pricing discoverability are NAMED follow-ups (no scarcity, no padding). No recurring wall → no harness proposal. Prior signal (run 41) retained below.
  # --- run 41 signal (retained) ---
  signal_run41: steady           # run 41: 1 shipped (#353 entitlement-reconciliation regression test), 0 abandoned, 0 circuit-breaker trips, 0 red REQUIRED CI checks, 0 review rejections (2/2 clean APPROVE with independent mutation-test proof). A QUIET late-CONVERGENCE run (§2 = SUCCESS): run 40's full 8-scout deep audit (same day) found a clean codebase; run 41 ran a FOCUSED 4-scout sweep of the newest surface and found it clean too. The one value-bar-clearing find was a §26 regression-net gap on a paid-entitlement path → #353.
  # --- run 40 signal (retained) ---
  signal_run40: steady           # run 40: 1 shipped (#351 org-endpoint hardening — concurrent-double-org integrity race + whitespace-name gap), 0 abandoned, 0 circuit-breaker trips, 1 review rejection (Reviewer B dropped a padding email-shape validator, resolved). The full 8-scout deep audit found a CLEAN codebase; the 5th scorecard's ship-critical gaps are RESOLVED on main (functional-reality by runs 36-39; seat-tier backend by #348); a scout false-positive on analytics guards was caught by verify-before-act. No recurring wall → no harness proposal. floor_met_year1 false. FYI prior cadence: run 39 = 1 shipped (improving, the 23×-stuck seat-tier backend), run 38 = 1 (steady), run 37 = 2, run 36 = 2, run 35 = 4.
```
