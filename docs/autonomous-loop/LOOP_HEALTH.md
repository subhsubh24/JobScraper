# LOOP HEALTH — is the LOOP getting better, not just busier?

Contract: the deep audit grades the PRODUCT (QUALITY_SCORECARD); this grades the LOOP.
Update the block below EVERY run in the ONE bookkeeping PR with REAL counts (from git/gh +
this run's record) — honest only (same anti-gaming rule as the business-case number; never
flatter the loop). Dashboard-readable. This is OBSERVABILITY, NOT a ship gate. CLASSIFY
every abandoned change so the loop never re-attempts the same dead-end. A `churning` or
`stuck` signal MUST trigger ONE "loop: harness improvement proposal" issue (the only channel
by which the loop's own rules improve — it cannot edit its routine or `.claude/`).

```yaml
LOOP_HEALTH:
  project: jobscraper
  as_of: 2026-07-12
  last_run: 2026-07-12            # run 45
  last_deep_audit: 2026-07-12    # run 43 (2026-07-12 04:44 UTC) ran the FULL ~8-scout Haiku deep audit. runs 44 + 45 (same day) were FOCUSED sweeps — cadence-aware (run 41 precedent): a full audit ran the same day, env unchanged (GEMINI+BROWSERBASE only), so a full re-run would only re-derive it. run 45 weighted at the newest surface (#368/#369 Margin telemetry) + a RE-ATTACK on the stuck store-readiness C. Next full deep audit due ~2026-07-13.
  this_run:                      # run 45
    changes_shipped: 2           # #370 Google Play feature graphic (the #1 ship-critical store-readiness gap: a 1024×500 no-alpha PNG REQUIRED to publish, rendered from reviewable HTML on the real brand tokens, guarded by tests/test_store_assets.py; clears THE DESIGNER QUESTION — broke a ~5× "store assets = design-core" defer by splitting the typographic BANNER from the bespoke-icon MARK) · #371 env-contract freshness + Margin self-validation (.env.example 6→~30 vars documented; declared the newest external dependency "margin" in the manifest; 9 fail-safe tests pinning "telemetry can never break the product" — #368/#369 shipped it under pragma-no-cover with zero guard). (+ this bookkeeping)
    changes_abandoned: 0         # 6-Haiku focused sweep: billing/org/auth CLEAN, business-case no honest buildable lever (floor live-data-blocked), functional-reality 2 findings DROPPED as over-flagged (graceful degradation of optional data), store + artifact-freshness surfaced the 2 shipped. No padding, no scarcity.
    abandoned_reasons: []        # reason ∈ gate_tsc|gate_pytest|gate_flake8|gate_lint|gate_build|review_value|review_correctness|circuit_breaker|conflict|dead_end|blocked_owner.
    verify_cycle_failures: 0     # 0 red REQUIRED CI checks at author time. #370 CI SUCCESS first attempt (merged); #371 auto-merging through the required gate. Local: flake8 repo-wide clean, margin 9/9 + store 2/2 pass, check_validation + --readiness pass, import asgi ok.
    review_findings_fixed: 1     # maker≠checker earned its keep: #371 Reviewer B caught a REAL doc-fact bug — .env.example said LOG_FORMAT defaults to human text, but logging_config.py:73 defaults to "json" (set plain for human) — backwards, in the file meant to be the trustworthy env contract. Fixed to match the code; a fresh scoped re-reviewer APPROVE.
    review_rejections: 0         # 1 REQUEST_CHANGES (#371 Reviewer B, cycle-1) actioned → the fix directly corrects the LOG_FORMAT default to match src/api/logging_config.py:73, re-verified by a fresh reviewer (a real doc-fact defect caught pre-merge, NOT an abandonment). All other reviewer passes APPROVE first time. 2 code PRs × 2 Sonnet reviewers + 1 scoped re-reviewer = 5 reviewer subagents.
    circuit_breaker_trips: 0
    incidents:                   # self-caught process notes (recovered, NOT abandonments)
      - "VERIFY-BEFORE-ACT averted 4 scout false-fixes (no padding): (1) two scouts wanted to 'correct the stale Expo-template icon claim' in ACCEPTANCE_AUDIT/BRAND_KIT — I checked the actual icon.png (chevron on a blueprint grid + sparkles) against the Expo SDK default: it IS the template, so the docs are CORRECT; rewriting them to 'bespoke' would have introduced a FALSE claim. (2) margin quality_method='heuristic' 'mislabel' — plausibly the intended semantics (how quality_score is measured is always the heuristic fit formula), owner telemetry, dropped. (3+4) coach + settings 'silent-fail' — graceful degradation of OPTIONAL data (the input always works; the honest empty-state already shows); an error banner would alarm users about a cosmetic fetch = worse UX. Over-flagged, dropped."
      - "maker≠checker (Reviewer A, #371) mutation-proved the margin fail-safe tests redden when the try/except is stripped, AND confirmed the scorer test patches scorer._meter not llm._meter — scorer imports _meter by name (from src.llm import _meter), so patching src.llm would be a silent no-op (a false-green trap the test correctly avoids). LESSON: patch a by-name-imported global on the module that USES it, not where it's defined."
      - "DESIGN ITERATION LOOP (SEE WHAT THE USER SEES): rendered the feature graphic 3× against my OWN vision review — v1 clipped the feature chips (content overflowed the 500px canvas), v2 lost them to a margin-top:auto interaction with the tall fit-badge, v3 fixed the vertical budget. A fixed-canvas store asset must be re-rendered + eyeballed until nothing clips — a DOM/spec check alone passes a clipped banner."
      - "GROUND-TRUTH re-probe: no new owner env this run (GEMINI + BROWSERBASE only; STRIPE price IDs / analytics / ESP / Turnstile still unset). Nightly RED by design (Career+ live price checks, unset secret; Gemini + Pro billing PASS) — known owner-block, not a regression; no fresh notification, no new issue. Margin (#368/#369) authored by a non-factory session; MARGIN_INGEST_* unset here so the meter is inert."
    deferred_decisions:          # named + deferred, NOT abandoned (value bar / feasibility / PMF-first — not dead-ends)
      - "Store SCREENSHOTS + bespoke app ICON (remaining store-readiness after the feature graphic): screenshots need a signed native build (not producible on Linux; web captures NOT substituted — a native listing must show the native app); the icon is a bespoke illustrative mark (owner/designer — auto-gen fails THE DESIGNER QUESTION). A3/G7 stay FAIL, ROADMAP box stays [ ]."
      - "Margin telemetry BLOCKING latency (#4): #369 made each emit blocking (2.0s timeout) because Vercel freezes daemon threads post-response. ≤4s on a 2-LLM-call endpoint when Margin is slow, but it SATISFIES the timeout rule (2.0s<60s), is inert without keys (unset in every deploy), and #369 was a deliberate recent owner decision — churning it was not a clear win. Named; revisit if keys go live and real latency appears."
      - "seat-tier MOBILE admin SURFACE — still native/Linux-unvalidatable. Business-case floor LIVE-DATA-BLOCKED: seat tier sellable end-to-end on web (backend #348 + /app/team #356 + /pricing #363) — crossing $100K needs real B2B adoption data, not more building. floor_met_year1 stays false, un-gamed."
      - "Track G/H (publishing-queue, experiment-engine, ASO/SEO plan, launch-plan doc, §11 media-gen) — SPECULATIVE pre-launch infra (0 users/0 channels); §9 PMF-first. annual-first/founder pricing — web/mobile UI + founder pricing LOWERS Y1 ARPA / unvalidatable. mobile IAP client — owner/native-blocked. Correctly deferred (43×)."
      - "OWNER-BLOCKED (PENDING_OPS, unchanged): the two ship-critical C's — business-case floor ($57.5K<$100K, needs live adoption data) + store-readiness (screenshots + icon after the feature graphic; mobile IAP) — plus the team/Career+ Stripe price secrets (nightly). NO new blocking external capability this run (margin declared non-blocking)."
  rolling_7d:
    merged_prs: 141            # repo-wide, approximate (137 through run 44 #367 + #368/#369 margin (non-factory session) + run 45 #370/#371); this bookkeeping merges after
    category_mix: {store-readiness: 1, artifact-freshness: 1}   # run 45: #370 (store feature graphic — #1 ship-critical gap) / #371 (env-contract freshness + Margin self-validation + fail-safe tests) — 2 file-disjoint PRs
    diversity: varied
    reverts: 0
    readiness_attempts: 0        # not attempted — the two ship-critical C's stay owner/GTM-blocked (business-case floor needs live adoption data + store assets/IAP/native captures), so submission-readiness is honestly not met
    readiness_rejected: 0
    recurring_failures: []       # the nightly Career+ red is a KNOWN owner-blocked external (missing secret), not a LOOP failure — tracked, not a stuck signal
    harness_proposals_open: 0     # #57 RESOLVED 2026-06-28 — gates now enforced as required checks
  enforced_in_ci: true           # required checks on main (preflight + web E2E), enforce_admins=ON
  auto_migrate_on_deploy: enabled # NO new migration this run. Head unchanged (a7d3e1f0c92b from run 31). Owner must still set Vercel DATABASE_URL→Neon before the next deploy (PENDING_OPS).
  validation:                    # self-validation manifest readiness (compute every run: scripts/check_validation.py --report)
    enforced_in_ci: true         # validate-capabilities runs inside the required `preflight` check
    capabilities_total: 10       # +1 — run 45 declared `margin` (Margin cost telemetry, #368/#369): validation=mock, blocking=false, covered_by tests/test_margin_telemetry.py.
    unmet: []                    # all external capabilities validated (real or mock); margin is inert-without-keys, non-blocking
  eval_coverage:                 # OUTPUT-quality coverage (compute: scripts/check_eval_coverage.py --report)
    enforced_in_ci: true         # check_eval_coverage runs inside the required `preflight` check
    ai_features_total: 5         # unchanged — run 44 touched the billing path, not an LLM feature.
    with_real_output_eval: 5     # each LLM feature judged on REAL Gemini output in CI (tests/evals/test_ai_output_evals.py)
  signal: improving              # bootstrapping | improving | steady | churning | stuck — run 45: 2 shipped (#370 store feature graphic, #371 env-contract + Margin self-validation), 0 abandoned, 0 circuit-breaker trips, 0 red REQUIRED CI checks at author time, 1 REQUEST_CHANGES (actioned → re-APPROVED), 0 rejections. 'improving' (not steady): BROKE a ~5× "store assets are design-core, defer" pattern on the #1 ship-critical gap (store-readiness C, "zero of 4 FAILs moved" for cycles) — by RE-EXAMINING the lump and shipping the loop-buildable half (a typographic Google Play feature graphic, REQUIRED to publish) while honestly keeping the owner/native half (screenshots + bespoke icon) deferred — same "re-attack a stuck defer when the sub-problem is actually buildable" move as run 39 (23× seat backend) and run 42 (3× seat surface). Plus #371 hardened the newest surface (#368/#369 Margin telemetry shipped with no test + no manifest entry → declared + 9 fail-safe guards) and fixed a real .env.example deploy hazard (6→~30 vars). maker≠checker earned its keep on a REAL doc-fact bug (LOG_FORMAT default backwards, Reviewer B → fixed → re-review APPROVE). VERIFY-BEFORE-ACT averted 4 scout false-fixes (the biggest: two scouts wanted to rewrite the "Expo template icon" docs to "bespoke" — but the icon IS the Expo default, so the docs are correct; the "fix" would have been a false claim). SEE-WHAT-THE-USER-SEES: iterated the graphic render 3× against my own vision review until nothing clipped. The two ship-critical C's remain (business-case floor live-data-blocked; store-readiness now needs only screenshots+icon after the feature graphic + mobile IAP); floor_met_year1 false, un-gamed. No recurring wall → no harness proposal.
  # --- run 44 signal (retained) ---
  signal_run44: steady           # run 44: 2 shipped (#365 Stripe timeout → honest 503 completing #361's uncaught-throw, #366 no-bypass guard), 0 abandoned, 0 CB trips, 1 REQUEST_CHANGES (actioned → resolved), 0 rejections. A FOCUSED late-convergence sweep ~7.5h after run 43's full deep audit (cadence-aware). maker≠checker caught a REAL vacuous-test defect (#366 Guard 2's whole-file check couldn't fail for billing.py — fixed to a call-site proximity check). Self-caught a reviewer shared-working-tree contamination before commit.
  # --- run 43 signal (retained) ---
  signal_run43: improving        # run 43: 3 shipped (#361 billing-path robustness, #362 auth eager-load, #363 /pricing Team discoverability), 0 abandoned, 0 CB trips, 0 red REQUIRED checks, 2 REQUEST_CHANGES (both actioned → re-APPROVED), 0 rejections. A FULL 8-scout deep-audit run that shipped the MAXIMAL file-disjoint value-bar set (3 PRs / 4 findings). maker≠checker caught 2 real defects pre-merge (a process-global-singleton guard-test isolation hole; a perf-scope overclaim); averted a scout FALSE-FIX (stripe create() has no timeout kwarg → used new_default_http_client). Seat tier now sellable end-to-end on web; $100K floor LIVE-DATA-BLOCKED.
  # --- run 42 signal (retained) ---
  signal_run42: improving        # run 42: 1 shipped (#356 seat-tier WEB management surface /app/team), 0 abandoned, 0 CB trips, 3 review findings fixed (B's must-fix on a structurally-untested member-mgmt path + 2 nits), 0 rejections. Broke a 3× deferral of the lowest incomplete ROADMAP item by RE-PROBING a stale 'can't validate the web-E2E gate locally' premise (now false). Org tier made user-reachable (was dead code — backend redirected to a /app/team page that didn't exist).
  # --- run 41 signal (retained) ---
  signal_run41: steady           # run 41: 1 shipped (#353 entitlement-reconciliation regression test), 0 abandoned, 0 circuit-breaker trips, 0 red REQUIRED CI checks, 0 review rejections (2/2 clean APPROVE with independent mutation-test proof). A QUIET late-CONVERGENCE run (§2 = SUCCESS): run 40's full 8-scout deep audit (same day) found a clean codebase; run 41 ran a FOCUSED 4-scout sweep of the newest surface and found it clean too. The one value-bar-clearing find was a §26 regression-net gap on a paid-entitlement path → #353.
  # --- run 40 signal (retained) ---
  signal_run40: steady           # run 40: 1 shipped (#351 org-endpoint hardening — concurrent-double-org integrity race + whitespace-name gap), 0 abandoned, 0 circuit-breaker trips, 1 review rejection (Reviewer B dropped a padding email-shape validator, resolved). The full 8-scout deep audit found a CLEAN codebase; the 5th scorecard's ship-critical gaps are RESOLVED on main (functional-reality by runs 36-39; seat-tier backend by #348); a scout false-positive on analytics guards was caught by verify-before-act. No recurring wall → no harness proposal. floor_met_year1 false. FYI prior cadence: run 39 = 1 shipped (improving, the 23×-stuck seat-tier backend), run 38 = 1 (steady), run 37 = 2, run 36 = 2, run 35 = 4.
```
