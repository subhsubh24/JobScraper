# LOOP HEALTH — is the LOOP getting better, not just busier?

Contract: the deep audit grades the PRODUCT (QUALITY_SCORECARD); this grades the LOOP.
Update the block below EVERY run in the ONE bookkeeping PR with REAL counts (from git/gh +
this run's record) — honest only (same anti-gaming rule as the business-case number; never
flatter the loop). Dashboard-readable. This is OBSERVABILITY, NOT a ship gate. CLASSIFY
every abandoned change so the loop never re-attempts the same dead-end. A `churning` or
`stuck` signal MUST trigger ONE "loop: harness improvement proposal" issue (the only channel
by which the loop's own rules improve — it cannot edit its routine or `.claude/`).

```yaml
LOOP_HEALTH:
  project: jobscraper
  as_of: 2026-07-12
  last_run: 2026-07-12            # run 44
  last_deep_audit: 2026-07-12    # run 43 (2026-07-12 04:44 UTC) ran the FULL ~8-scout Haiku deep audit. run 44 (this run, ~7.5h later) was a FOCUSED 5-scout sweep — cadence-aware (run 41 precedent): a full audit ran the same day, HEAD + env unchanged since, so a re-run would only re-derive it. Next full deep audit due ~2026-07-13.
  this_run:                      # run 44
    changes_shipped: 2           # #365 Stripe checkout timeout → clean 503 (COMPLETES #361: the bounded 25s timeout raised an APIConnectionError that escaped UNCAUGHT → generic 500; now caught → honest retryable 503 'no charge was made' in BOTH checkout endpoints; narrow catch so config errors still 500) · #366 Stripe no-bypass source-scan guard (a raw checkout call may live ONLY in the two configure_stripe()-bounding modules; per-PR guard modeled on #339, closes the gap that #361 only protected the 2 existing sites). (+ this bookkeeping)
    changes_abandoned: 0         # Focused 5-scout sweep → 4 of 5 clean/owner-blocked (correctness clean, security clean, functional-reality 0 dead-ends, business-case no honest buildable lever); the tests-evals scout surfaced the one real value-bar find (#361's uncaught-throw follow-up). No padding, no scarcity.
    abandoned_reasons: []        # reason ∈ gate_tsc|gate_pytest|gate_flake8|gate_lint|gate_build|review_value|review_correctness|circuit_breaker|conflict|dead_end|blocked_owner.
    verify_cycle_failures: 0     # 0 red REQUIRED CI checks at author time. Both PRs' ci.yml (preflight + web-E2E journeys) concluded SUCCESS first attempt; auto-merged through the required gate (no --admin bypass). Local: flake8 clean, full non-live suite 619 passed @ 91% cov (floor 88), every new test mutation-proven load-bearing.
    review_findings_fixed: 3     # maker≠checker earned its keep on a REAL defect: #366 Reviewer A mutation-proved Guard 2's whole-file "configure_stripe(" check was VACUOUS for src/billing.py (the file DEFINES + calls it, so the substring is present even if the call site drops the bounding call) → FIXED with a call-site proximity check (20-line window, def-excluded), re-verified reddening on the reviewer's exact repro. Plus 2 non-blocking #365 hardening suggestions INCORPORATED: a narrow-scope pin (a config AuthenticationError still 500s, not laundered to 503) + an org "no fake seat grant" assertion.
    review_rejections: 0         # 1 REQUEST_CHANGES (#366, cycle-1) actioned → the fix directly implements the reviewer's requested proximity check + is mutation-verified against their exact repro (a real vacuous-test defect caught pre-merge, NOT an abandonment). All other reviewer passes APPROVE first time. 2 code PRs × 2 Sonnet reviewers = 4 reviewer subagents.
    circuit_breaker_trips: 0
    incidents:                   # self-caught process notes (recovered, NOT abandonments)
      - "maker≠checker CAUGHT A REAL VACUOUS-TEST DEFECT (#366): the no-bypass Guard 2's whole-file `\"configure_stripe(\" in text` check can NEVER fail for src/billing.py, because that file both DEFINES (`def configure_stripe():`) and CALLS the symbol — so removing the actual bounding call at the checkout site (the exact #361 regression) left both tests green (Reviewer A mutation-proved it). Fixed to a call-site proximity check. LESSON: a source-scan guard on a symbol DEFINED+CALLED in the same file must validate the CALL SITE, not whole-file presence — else it silently guards nothing."
      - "VERIFY-BEFORE-ACT (SDK): confirmed against the installed stripe-python 7.9.0 that a bounded-timeout / connection failure raises stripe.error.APIConnectionError (RequestsClient._handle_request_error wraps requests Timeout/ConnectionError), and that it is a SIBLING of AuthenticationError/InvalidRequestError under StripeError — so `except APIConnectionError` is the correct, narrow target (config errors still 500). Reviewer A independently reproduced this against the SDK."
      - "SHARED-WORKING-TREE HAZARD (self-caught): a Change-1 reviewer subagent ran `git stash`/`pop` in the shared checkout to test load-bearing, leaking Change-1's billing.py edit onto branch 2's working tree. Caught via `git status` (modified billing.py on a test-only branch), verified the diff was exactly the leak (no stashes remained), `git restore`d before any commit. LESSON: reviewer subagents share the maker's working copy — verify tree cleanliness before each commit."
      - "GROUND-TRUTH re-probe: no new owner env this run (GEMINI + BROWSERBASE only; STRIPE price IDs / analytics / ESP / Turnstile still unset). Nightly RED by design (4 failed / 17 passed — Career+ live billing price checks, unset secret; Gemini + Pro billing PASS) — identical to runs 38-43, a known owner-block (PENDING_OPS `stripe-account`), not a regression; no fresh notification, no new issue."
    deferred_decisions:          # named + deferred, NOT abandoned (value bar / feasibility / PMF-first — not dead-ends)
      - "login()/register() eager-load (both #362 reviewers named it): login() has the identical lazy user.subscription via user_public, and register() mints a subscription-less user (lazy read → None, not cleanly joinedload-able at construction). Once-per-session paths, lower value than the every-request token choke-point already fixed. Named follow-up, not scarcity."
      - "register postSignupPath backslash open-redirect nuance (#363 Reviewer A, INFORMATIONAL, pre-existing): the ?next guard rejects // but not backslash variants (/\\evil.com) some browsers normalize to protocol-relative. NOT introduced by #363 (which passes a fixed literal). Worth a follow-up hardening ticket; out of scope this run."
      - "2 design-consistency nits (design scout): Team/Settings h1 font-bold vs font-extrabold elsewhere; job-detail subtitle missing mt-1 text-sm. Borderline-cosmetic (§5 lists font-weight/margin tweaks as churn) — SKIPPED to avoid padding; revisit only if bundled into a real surface change."
      - "seat-tier MOBILE admin SURFACE — still native/Linux-unvalidatable (deferred). Business-case floor now LIVE-DATA-BLOCKED: the seat tier is sellable end-to-end on web (backend #348 + /app/team #356 + /pricing discoverability #363) — crossing $100K needs real B2B adoption data, not more building. floor_met_year1 stays false (honest, un-gamed)."
      - "publishing-queue + experiment-engine + ASO/SEO plan + launch-plan doc + §11 media-gen adapter (Track G/H) — SPECULATIVE pre-launch infra (0 users/0 traffic/0 channels). §9 PMF-first: fix the PRODUCT, not acquisition plumbing nothing consumes. Perf/Track-G-H scout independently re-classified as padding. Correctly deferred 42×."
      - "annual-first / founder pricing — annual-first is web/mobile UI (+$2–5K est); founder pricing LOWERS Y1 ARPA + unvalidatable pre-launch (gaming to credit). Named; not this run. mobile IAP client + store assets stay owner/native/design-core (blocked_owner)."
      - "OWNER-BLOCKED (PENDING_OPS, unchanged): the two ship-critical C's — business-case floor ($57.5K<$100K, needs live adoption data) + store-readiness (rendered assets, mobile IAP, native captures) — plus the team/Career+ Stripe price secrets (nightly). NO new external capability this run."
  rolling_7d:
    merged_prs: 137            # repo-wide, approximate (135 through run 43 #364 + run 44 #365/#366); this bookkeeping merges after
    category_mix: {correctness: 1, tests-evals: 1}   # run 44: #365 (correctness/reliability — Stripe timeout → honest 503) / #366 (tests-evals — no-bypass source-scan guard) — 2 file-disjoint PRs
    diversity: varied
    reverts: 0
    readiness_attempts: 0        # not attempted — the two ship-critical C's stay owner/GTM-blocked (business-case floor needs live adoption data + store assets/IAP/native captures), so submission-readiness is honestly not met
    readiness_rejected: 0
    recurring_failures: []       # the nightly Career+ red is a KNOWN owner-blocked external (missing secret), not a LOOP failure — tracked, not a stuck signal
    harness_proposals_open: 0     # #57 RESOLVED 2026-06-28 — gates now enforced as required checks
  enforced_in_ci: true           # required checks on main (preflight + web E2E), enforce_admins=ON
  auto_migrate_on_deploy: enabled # NO new migration this run. Head unchanged (a7d3e1f0c92b from run 31). Owner must still set Vercel DATABASE_URL→Neon before the next deploy (PENDING_OPS).
  validation:                    # self-validation manifest readiness (compute every run: scripts/check_validation.py --report)
    enforced_in_ci: true         # validate-capabilities runs inside the required `preflight` check
    capabilities_total: 9        # unchanged — run 44's two PRs are billing-path robustness + a test-only regression guard, no new external capability.
    unmet: []                    # all external capabilities validated (real or mock)
  eval_coverage:                 # OUTPUT-quality coverage (compute: scripts/check_eval_coverage.py --report)
    enforced_in_ci: true         # check_eval_coverage runs inside the required `preflight` check
    ai_features_total: 5         # unchanged — run 44 touched the billing path, not an LLM feature.
    with_real_output_eval: 5     # each LLM feature judged on REAL Gemini output in CI (tests/evals/test_ai_output_evals.py)
  signal: steady                 # bootstrapping | improving | steady | churning | stuck — run 44: 2 shipped (#365 Stripe timeout → honest 503, #366 no-bypass guard), 0 abandoned, 0 circuit-breaker trips, 0 red REQUIRED CI checks at author time, 1 REQUEST_CHANGES (actioned → resolved), 0 rejections. 'steady' (not improving): a FOCUSED late-convergence sweep ~7.5h after run 43's full deep audit (cadence-aware, run 41 precedent — a full re-run would only re-derive it), 4 of 5 scouts clean/owner-blocked, the honest buildable set was 2 real disjoint PRs — a genuine BUILDS≠WORKS follow-up completing #361 (a bounded timeout whose failure escaped uncaught as a 500) + its per-PR no-bypass guard. NOT padding, NOT scarcity (§2: a quiet coherent run is a SUCCESS). maker≠checker earned its keep on a REAL vacuous-test defect (#366 Guard 2's whole-file check couldn't fail for billing.py — mutation-proven by Reviewer A, fixed to a call-site proximity check). Verify-before-act confirmed the exact Stripe exception against the installed SDK before coding the catch. Self-caught a shared-working-tree contamination (reviewer subagent's git stash) before it hit a commit. The two ship-critical C's stay owner/live-data-blocked; floor_met_year1 false, un-gamed. Named follow-ups re-confirmed sub-bar by the focused scouts (login/register eager-load = perf-only; register backslash-redirect = non-exploitable via router.push; design nits cosmetic). No recurring wall → no harness proposal. Prior signals (runs 43/42) retained below.
  # --- run 43 signal (retained) ---
  signal_run43: improving        # run 43: 3 shipped (#361 billing-path robustness, #362 auth eager-load, #363 /pricing Team discoverability), 0 abandoned, 0 CB trips, 0 red REQUIRED checks, 2 REQUEST_CHANGES (both actioned → re-APPROVED), 0 rejections. A FULL 8-scout deep-audit run that shipped the MAXIMAL file-disjoint value-bar set (3 PRs / 4 findings). maker≠checker caught 2 real defects pre-merge (a process-global-singleton guard-test isolation hole; a perf-scope overclaim); averted a scout FALSE-FIX (stripe create() has no timeout kwarg → used new_default_http_client). Seat tier now sellable end-to-end on web; $100K floor LIVE-DATA-BLOCKED.
  # --- run 42 signal (retained) ---
  signal_run42: improving        # run 42: 1 shipped (#356 seat-tier WEB management surface /app/team), 0 abandoned, 0 CB trips, 3 review findings fixed (B's must-fix on a structurally-untested member-mgmt path + 2 nits), 0 rejections. Broke a 3× deferral of the lowest incomplete ROADMAP item by RE-PROBING a stale 'can't validate the web-E2E gate locally' premise (now false). Org tier made user-reachable (was dead code — backend redirected to a /app/team page that didn't exist).
  # --- run 41 signal (retained) ---
  signal_run41: steady           # run 41: 1 shipped (#353 entitlement-reconciliation regression test), 0 abandoned, 0 circuit-breaker trips, 0 red REQUIRED CI checks, 0 review rejections (2/2 clean APPROVE with independent mutation-test proof). A QUIET late-CONVERGENCE run (§2 = SUCCESS): run 40's full 8-scout deep audit (same day) found a clean codebase; run 41 ran a FOCUSED 4-scout sweep of the newest surface and found it clean too. The one value-bar-clearing find was a §26 regression-net gap on a paid-entitlement path → #353.
  # --- run 40 signal (retained) ---
  signal_run40: steady           # run 40: 1 shipped (#351 org-endpoint hardening — concurrent-double-org integrity race + whitespace-name gap), 0 abandoned, 0 circuit-breaker trips, 1 review rejection (Reviewer B dropped a padding email-shape validator, resolved). The full 8-scout deep audit found a CLEAN codebase; the 5th scorecard's ship-critical gaps are RESOLVED on main (functional-reality by runs 36-39; seat-tier backend by #348); a scout false-positive on analytics guards was caught by verify-before-act. No recurring wall → no harness proposal. floor_met_year1 false. FYI prior cadence: run 39 = 1 shipped (improving, the 23×-stuck seat-tier backend), run 38 = 1 (steady), run 37 = 2, run 36 = 2, run 35 = 4.
```
