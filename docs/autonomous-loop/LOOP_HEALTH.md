# LOOP HEALTH — is the LOOP getting better, not just busier?

Contract: the deep audit grades the PRODUCT (QUALITY_SCORECARD); this grades the LOOP.
Update the block below EVERY run in the ONE bookkeeping PR with REAL counts (from git/gh +
this run's record) — honest only (same anti-gaming rule as the business-case number; never
flatter the loop). Dashboard-readable. This is OBSERVABILITY, NOT a ship gate. CLASSIFY
every abandoned change so the loop never re-attempts the same dead-end. A `churning` or
`stuck` signal MUST trigger ONE "loop: harness improvement proposal" issue (the only channel
by which the loop's own rules improve — it cannot edit its routine or `.claude/`).

```yaml
LOOP_HEALTH:
  project: jobscraper
  as_of: 2026-07-03
  last_run: 2026-07-03            # run 20
  last_deep_audit: 2026-07-03    # run 18's full sweep was the last FULL deep audit (same day); runs 19+20 ran focused sweeps (< 24h since run 18). Run 20's freshest signal was owner-filed issue #222 (final-audit) + an orphaned PR #216.
  this_run:                      # run 20
    changes_shipped: 3           # #216 §32 signup guard (RESURRECTED orphan), #228 §28 DB fail-loud (data-loss prevent), #229 billing every-plan live coverage (+ this bookkeeping)
    changes_abandoned: 0
    abandoned_reasons: []        # [{change, reason}] reason ∈ gate_tsc|gate_pytest|gate_flake8|gate_lint|gate_build|review_value|review_correctness|circuit_breaker|conflict|dead_end|blocked_owner
    verify_cycle_failures: 1     # #216: the resurrected test PASSED in isolation but FAILED in the full suite — career_operator logger left .disabled=True by alembic fileConfig(disable_existing_loggers=True) when test_migrations runs first → logger.warning a silent no-op → caplog empty. DEEP-DIAGNOSED (printed logger state in the failing ordering) + fixed the CAPTURE robustly (direct handler + force disabled=False). 1 verify cycle, within ≤2.
    review_rejections: 0         # all 6 Sonnet reviewers APPROVED first pass (tightly-scoped, pre-verified PRs); Reviewer A/#216 mutation-verified the §32 guard load-bearing, Reviewer B/#216 empirically reproduced the exact fileConfig flake mechanism, Reviewer A/#228 confirmed no CI env sets VERCEL so the guard is inert in the pipeline.
    circuit_breaker_trips: 0
    incidents:                   # self-caught process notes (recovered, NOT abandonments)
      - "ORPHANED-PR / bookkeeping-lied-about-a-merge (HEADLINE): run-18 loop-memory recorded PR #216 as MERGED, but it was still OPEN and the vulnerable code was live on main (asgi.py:662 unwrapped analytics.record_event). The non-merge cause was a full-suite-only test flake that kept CI red. RULE: never trust a loop-memory 'merged' claim — grep the change on HEAD to confirm it landed. Resurrected the orphan (rebase + re-verify + 2 fresh reviewers) rather than rebuild."
      - "DEEP DIAGNOSIS on a test flake (not delete-to-green): the #216 log assertion flaked ONLY in the full suite. Observed the real system (printed career_operator.disabled inside the failing ordering) → alembic fileConfig disables the logger → fixed the CAPTURE (direct handler + disabled=False), not the assertion. A pass-alone/flake-in-suite test is a REAL defect (it's why the PR never merged), not noise."
      - "SKEPTICISM refuted the functional-reality scout's only 'CRITICAL': mobile coach.tsx:52 useState<string>(newSessionId) is React's LAZY-INITIALIZER idiom (React calls it once on mount), NOT a bug — multi-turn coaching works. Verified vs the code before trusting the scout. Core web+mobile loops sound (matches run 18)."
    deferred_decisions:          # named + deferred, NOT abandoned (DECISION COROLLARY / value bar / brakes — not dead-ends)
      - "issue #222 DEAD-CODE items (anti-padding): get_session_summary None-guard + generate_study_plan/generate_cover_letter wire-or-drop are on UNROUTED code — a guard+test on unreachable code is marginal, and deleting built+tested features is a decision, not loop work (run 12/14 precedent). Named + deferred, not shipped as padding."
      - "TEAM/B2B2C seat tier — the ONLY remaining unbuilt named floor-lever (Career+/annual-first/referral all already built). A genuine MULTI-RUN epic (org+seat model, seat billing, composable entitlement); crediting seat ARR at 0 users = gaming, so it does NOT honestly flip $100K pre-launch. The right next big build."
      - "login-lockout cross-instance; native-mobile component snapshots + web SCREENSHOT REGEN (design-taste A→A+); Pro→Career+ in-place upgrade via Stripe portal; /api/analytics/pipeline pagination + GET /api/jobs/{id} selectinload (pre-launch 0-row polish)."
      - "OWNER-BLOCKED (PENDING_OPS): set Vercel DATABASE_URL→Neon BEFORE next deploy (#228 now fail-loud — app refuses to boot without it, mirrors JWT_SECRET); STRIPE_PRICE_CAREERPLUS_* secrets (#229 nightly reddens without them); mobile IAP client; store asset images; CAPTCHA keys; connect a real ESP; REQUIRE_LIVE_TESTS=1 in nightly.yml."
  rolling_7d:
    merged_prs: 67             # repo-wide across all routines; incl. this run's #216/#228/#229 (+ this bookkeeping)
    reverts: 0
    readiness_attempts: 0
    readiness_rejected: 0
    recurring_failures: []
    harness_proposals_open: 0     # #57 RESOLVED 2026-06-28 — gates now enforced as required checks
  enforced_in_ci: true           # required checks on main (preflight + web E2E), enforce_admins=ON
  auto_migrate_on_deploy: enabled # NO new migration this run (all 3 changes are app-code/tests only). Latest migration remains d4e7a1c9b8f2.
  validation:                    # self-validation manifest readiness (compute every run: scripts/check_validation.py --report)
    enforced_in_ci: true         # validate-capabilities runs inside the required `preflight` check
    capabilities_total: 8        # +captcha (run 19): Turnstile TURNSTILE_SECRET declared, validation:mock (siteverify round-trip mocked), blocking:false
    unmet: []                    # all external capabilities validated (real or mock)
  eval_coverage:                 # OUTPUT-quality coverage (compute: scripts/check_eval_coverage.py --report)
    enforced_in_ci: true         # check_eval_coverage runs inside the required `preflight` check
    ai_features_total: 3         # fit-scoring, prep-pack, coach
    with_real_output_eval: 3     # each judged on REAL Gemini output in CI (tests/evals/test_ai_output_evals.py)
  signal: steady                 # bootstrapping | improving | steady | churning | stuck — run 20: 3 shipped, 0 abandoned, 0 circuit-breaker trips, 0 review rejections (all 6 reviewers APPROVE first pass), 1 verify cycle (the #216 full-suite flake, deep-diagnosed). Headline: the biggest win was a DEEP-DIAGNOSIS + honest-bookkeeping catch — run-18 loop-memory recorded PR #216 as merged but it was ORPHANED (still open, the §32 fix absent from main) because a full-suite-only test flake (career_operator logger .disabled=True from alembic fileConfig) kept CI red; observed the real logger state in the failing ordering, fixed the capture robustly, and resurrected the PR rather than rebuild. Also closed issue #222's CRITICAL (Career+ checkout 503 → #229 nightly regression guard over ALL plans) + MED data-loss (#228 §28 fail-loud on serverless-without-Postgres). Anti-padding held: refuted the scout's only 'CRITICAL' (React lazy-initializer, not a bug) and deferred #222's dead-code LOW/MED items (guard+test on unreachable code = padding; deleting built features = a decision). floor_met_year1 stays false (no revenue lever; team/B2B2C is the named path). No recurring wall → no harness proposal warranted. Prior run 19: 2 shipped, 0 abandoned. On a MATURE repo the honest maximal set was 2 value-bar-clearing changes — a stale-scorecard reconcile against HEAD showed ~4 named gaps ALREADY closed (390px, create_job dedup, embedding cache, analytics N+1, Career+/annual-first/referral), so building them would have been padding; the one remaining ship-critical business-case lever (team/B2B2C seat tier) is a genuine multi-run epic + store-readiness is owner-core, so 2 changes is NOT artificial scarcity. Shipped: #226 CAPTCHA/Turnstile bot-protection seam (closes the open Track F box; DISABLED-by-default, degrades honestly; one of two security A→A+ items — lockout-cross-instance remains, deliberately), #225 a net-new scorer configured-but-failing-embedding guard-test. maker≠checker earned its keep AGAIN: caught a native-mobile-auth-outage ordering hazard (#226) and a duplicate-test-with-false-claim (#225), both invisible from green tests. floor_met_year1 stays false (no revenue lever this run; team/B2B2C is the named path). No recurring wall → no harness proposal warranted. Prior run 18 headline: maker≠checker caught a REAL defect in BOTH backend PRs (a Postgres transaction-poisoning completeness gap in the §32 signup guard #216, and a duplicate-test-with-a-false-claim in #217 — the scorecard's zero-vector correctness top_gap is stale/already-covered), both invisible from green tests and both fixed within the ≤2-cycle cap on a reviewer-prescribed fix (convergence, not thrash). Shipped: #212 §32 signup resilience (no non-essential side-effect can hard-block account creation, rollback-safe on Postgres), scorer structured logging (print→career_operator.scorer), web job-detail role=alert a11y. Anti-padding held (rejected embedding-cache dead-end, stale 390px screenshot, intentional border-focus inputs, cross-instance login-lockout, duplicate prep eval; refuted the AI-consent-modal-hang false positive). No revenue lever (floor_met_year1 stays false; TEAM/B2B2C remains the named floor-lever); no recurring wall → no harness proposal warranted. Prior run 17 headline retained below for history: closed the audit-filed §28 synthetic-green issue #197 on two fronts — (1) the nightly `live` real-service lane now FAILS-not-skips when its key is unexpectedly absent (tests/live_guard.py; owner sets REQUIRE_LIVE_TESTS in nightly.yml to arm it), and (2) added a REAL SMTP delivering backend to the email seam (previously dryrun/capture only) that is honest (delivered=True only on server-accept; fail-loud on incomplete config; inert by default) and CI-validated via monkeypatched smtplib — so a connected owner just sets SMTP_* env. Also fixed a scorecard-named correctness gap: create_job is now idempotent (identical re-submit returns the existing job, no dup row / no double paid-rescore / no double usage-count / no double analytics). ANTI-PADDING held (rejected speculative pagination + a restrictive coach counter in favor of the honest CORRECT-DOCS fix). Bookkeeping aligned 3 stale doc claims (issue #192). No recurring wall → no harness proposal warranted. HONEST on the number: no revenue lever this run; floor_met_year1 stays false; TEAM/B2B2C remains the primary named floor-lever (doesn't flip the floor at 0 users). No recurring wall → no harness proposal warranted.
```
