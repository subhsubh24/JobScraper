# LOOP HEALTH — is the LOOP getting better, not just busier?

Contract: the deep audit grades the PRODUCT (QUALITY_SCORECARD); this grades the LOOP.
Update the block below EVERY run in the ONE bookkeeping PR with REAL counts (from git/gh +
this run's record) — honest only (same anti-gaming rule as the business-case number; never
flatter the loop). Dashboard-readable. This is OBSERVABILITY, NOT a ship gate. CLASSIFY
every abandoned change so the loop never re-attempts the same dead-end. A `churning` or
`stuck` signal MUST trigger ONE "loop: harness improvement proposal" issue (the only channel
by which the loop's own rules improve — it cannot edit its routine or `.claude/`).

```yaml
LOOP_HEALTH:
  project: jobscraper
  as_of: 2026-07-09
  last_run: 2026-07-09            # run 35 (run 34's bookkeeping did NOT update this block — it was stale at run 33; corrected here)
  last_deep_audit: 2026-07-09    # run 35 ran a FULL 8-scout Haiku deep audit (functional-reality / backend-correctness+security / store-readiness / business-case adversarial / quality-grade-reconcile / design+visual / perf+dead-code / growth+marketing+artifact-freshness). run 34 also ran a full 8-scout sweep; runs 31–33 were lighter.
  this_run:                      # run 35
    changes_shipped: 4           # #320 ATS import UI (RECOVERED — run 34 claimed it "shipped" but its web-E2E was RED and it never merged; fixed the getByLabel(/Company/i) collision, rebased, E2E 20/20, merged) · #322 per-user rate limiting on 11 authed endpoints (security; asgi.py + parametrized regression) · #323 dead-code retirement (cli.py/src/main.py/src/crm, −614 LOC) · #324 store AI-disclosure embedded in ASO_COPY Full description — all file-DISJOINT (web / asgi+test / cli+setup.* / ASO_COPY) (+ this bookkeeping)
    changes_abandoned: 0         # coverage-floor 85→88 was DEFERRED (not abandoned): I mis-measured actual coverage as 86% via --cov=. over the whole repo; a reviewer + I re-confirmed the CI-configured actual is 91.82%, so the 88 bump is VIABLE — named for a clean standalone next-run PR (collided on setup.cfg with #323 this run).
    abandoned_reasons: []        # reason ∈ gate_tsc|gate_pytest|gate_flake8|gate_lint|gate_build|review_value|review_correctness|circuit_breaker|conflict|dead_end|blocked_owner.
    verify_cycle_failures: 0     # 0 red REQUIRED CI checks on the changes I authored. Ran the EXACT required gates locally before every push (backend pytest 567@ + flake8; web tsc/eslint/next-build; the FULL web E2E 20/20 via the preinstalled Chromium-1194 executablePath + --no-sandbox, since @playwright/test wants build 1228). NB: #320's web-E2E had been red the WHOLE prior day (a run-34 gap) — recovered here.
    review_rejections: 3         # #320 both APPROVE. #322: BOTH REQUEST_CHANGES — test exercised only 2/11 endpoints vs a docstring claiming all 11 (→ parametrized all 11) + an undisclosed invalid-token-flood trade-off (→ documented). #323: BOTH REQUEST_CHANGES — Reviewer B: setup.sh `python cli.py init` under set -e breaks onboarding (→ scripts/init_db.py + real next-steps + dropped orphaned deps); Reviewer A: my OWN "~86%" coverage comment was wrong (real CI cov 91.82%) (→ reverted). #324: BOTH REQUEST_CHANGES — false "one tap" report claim (2-step flow) + overstated policy mandate (→ both fixed). All fixed within ≤2 cycles → fresh re-reviewer APPROVE on each.
    circuit_breaker_trips: 0
    incidents:                   # self-caught + reviewer-caught process notes (recovered, NOT abandonments)
      - "\"merging\" ≠ \"merged\": run 34's bookkeeping (#321) titled 'import UI + perf shipped' and its loop-memory said #320 was 'merging' — but #320 NEVER merged (its required web-E2E was RED for a full day from a getByLabel(/Company/i) strict-mode collision). The marketed ATS import had NO UI on main while the ledger claimed it shipped. RULE reinforced: a claim ticks ONLY on PR merged:true + green required checks; I verified `git show origin/main:<file>` for every claim this run."
      - "maker≠checker EARNED ITS KEEP on ALL 3 non-trivial PRs: #322 (test covered 2/11 endpoints vs a docstring claiming 11 → parametrized all 11; + an undisclosed invalid-token-flood trade-off → documented); #323 (setup.sh `python cli.py init` under set -e would hard-fail onboarding after the deletion → fixed to scripts/init_db.py + real next-steps; AND a reviewer caught MY OWN bogus '~86%' coverage comment — I'd measured --cov=. over the whole repo, real CI cov is 91.82% → reverted); #324 (false 'one tap' report claim on a 2-step flow + overstated policy mandate → both fixed). Every PR had a real defect a reviewer caught; the value bar/gates alone would not have."
      - "Shared-tree hazard managed: reviewer subagents share the working dir, so I told them to review via refs (`git show <branch>:<file>`) or an isolated `git worktree`; several spawned their own worktrees for mutation-testing the PR2 reverts, keeping my main tree stable while I built the next PR. (Still hit one transient E2E boot failure when a reviewer briefly touched the tree — re-ran clean.)"
      - "BUILDS≠WORKS on the web E2E: the preinstalled Chromium is build 1194 but @playwright/test wants 1228, so a plain `playwright test` errors 'Executable doesn't exist … chrome-headless-shell-1228'. Ran the FULL suite 20/20 green via a throwaway config pointing executablePath at /opt/pw-browsers/chromium-1194/chrome-linux/chrome + --no-sandbox (root). This is the recovery recipe for the routine env."
      - "Coverage-measurement foot-gun (self-inflicted, reviewer-caught): `pytest --cov=.` measures the WHOLE repo (tests/scripts included) and reported 86%; the CI-configured coverage is `source=src,asgi` via `pytest -m \"not live\" --cov` = 91.82%. Always measure with the configured source / the exact preflight command, not --cov=."
    deferred_decisions:          # named + deferred, NOT abandoned (disjoint rule / value bar / brakes — not dead-ends)
      - "Coverage floor 85→88: VIABLE (CI-configured actual is 91.82%, ~7pt buffer). Not bundled this run — it collides on setup.cfg with the dead-code PR (#323) and a one-line ratchet didn't merit its own PR+2-reviewer cycle late in this run. Clean standalone next-run PR."
      - "§29 Browserbase deployed-app validator: keys (BROWSERBASE_API_KEY/PROJECT_ID) were SET this run, but with no reachable PROD_URL / no way to produce a REAL VALIDATOR_STATUS.md, building the harness would only yield a fabricated green (BUILDS≠WORKS forbids it). Build once a live app URL is available."
      - "§11 marketing media-gen adapter (Gemini key set) — pre-launch with 0 connected channels, staging mass creative is premature (growth scout agreed). /insights web E2E (marketed retention surface, no browser coverage; endpoint IS unit-tested). Visual-verification baseline refresh (web@390px; native captures owner-blocked). login-lockout cross-instance (deliberate; CAPTCHA is the fix). DNS-rebinding SSRF residual (low). Add-job tablist arrow-key a11y (Tab+Enter works; APG roving-tabindex is a nicety)."
      - "TEAM/B2B2C seat tier — the ONLY remaining unbuilt named floor-lever; genuinely owner/GTM-blocked (seat ARR uncreditable at 0 users = gaming; speculative B2B code pre-launch with 0 pipeline is the 'scale into a leaky bucket' §9 forbids — RE-CONFIRMED this run, 20th). Annual-first is honestly presented as an UNBUILT candidate, not an over-claim (BUSINESS_CASE left as-is)."
      - "OWNER-BLOCKED (PENDING_OPS, unchanged): the two ship-critical C's — business-case-strength floor + store-readiness (rendered assets, mobile IAP, native captures). GEMINI_API_KEY/Vercel DATABASE_URL→Neon/STRIPE_PRICE_*/RevenueCat/CAPTCHA keys/connect an ESP/REQUIRE_LIVE_TESTS=1/SITE GATE decision. NO new migration this run (no schema change)."
  rolling_7d:
    merged_prs: 118            # repo-wide, approximate (carried from 112 at run 33 + run 34's #319/#321 + this run's #320/#322/#323/#324); this bookkeeping merges after
    category_mix: {functional-reality: 1, security: 1, tech-debt: 1, store-readiness: 1}   # §37 diversity: import UI reachability / per-user rate-limit / dead-code retirement / store AI-disclosure — four distinct categories, no monoculture
    diversity: varied
    reverts: 0
    readiness_attempts: 0        # not attempted — the two ship-critical C's stay owner/GTM-blocked (business-case floor + store assets/IAP), so submission-readiness is honestly not met
    readiness_rejected: 0
    recurring_failures: []
    harness_proposals_open: 0     # #57 RESOLVED 2026-06-28 — gates now enforced as required checks
  enforced_in_ci: true           # required checks on main (preflight + web E2E), enforce_admins=ON
  auto_migrate_on_deploy: enabled # NO new migration this run. Head unchanged (a7d3e1f0c92b from run 31). Owner must still set Vercel DATABASE_URL→Neon before the next deploy (PENDING_OPS).
  validation:                    # self-validation manifest readiness (compute every run: scripts/check_validation.py --report)
    enforced_in_ci: true         # validate-capabilities runs inside the required `preflight` check
    capabilities_total: 9        # unchanged — readiness is KEY-FREE (no external secret, no LLM); ingestion is code-only. No new external capability introduced.
    unmet: []                    # all external capabilities validated (real or mock)
  eval_coverage:                 # OUTPUT-quality coverage (compute: scripts/check_eval_coverage.py --report)
    enforced_in_ci: true         # check_eval_coverage runs inside the required `preflight` check
    ai_features_total: 5         # unchanged — no new LLM feature this run (import UI is client wiring; rate-limit keying + dead-code + store copy are not model features).
    with_real_output_eval: 5     # each LLM feature judged on REAL Gemini output in CI (tests/evals/test_ai_output_evals.py)
  signal: improving              # bootstrapping | improving | steady | churning | stuck — run 35: 4 shipped, 0 abandoned, 0 circuit-breaker trips, 0 red REQUIRED CI checks on authored changes. CENTERPIECE recovered a run-34 integrity gap: the marketed ATS import UI (#320) that run-34 CLAIMED shipped had actually never merged (its required web-E2E was red all day) — fixed the label collision + rebased + full E2E 20/20 + merged, so the claim is now TRUE. Plus a security hardening (#322 per-user rate limiting on 11 authed endpoints, fixing shared-NAT false-429), a −614-LOC dead-code retirement (#323), and a store AI-disclosure fix (#324). maker≠checker EARNED ITS KEEP on all 3 non-trivial PRs (test overclaim, a setup.sh onboarding breakage, my own bogus coverage comment, a false 'one tap' claim) — every one fixed within ≤2 cycles + fresh re-review. Honest scope: the SAME two owner/GTM-blocked ship-critical C's remain (business-case floor $57.5K<$100K — RE-CONFIRMED 20th run; store-readiness assets+IAP), so the run drove the highest-value LOOP-buildable work + closed a real functional-reality integrity gap. Key lesson banked: verify PR merged:true before claiming shipped ('merging' ≠ 'merged'). No recurring wall → no harness proposal. floor_met_year1 stays false. FYI prior cadence: run 34 = 2 shipped, run 33 = 3, run 32 = 4.
```
