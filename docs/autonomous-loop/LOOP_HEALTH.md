# LOOP HEALTH — is the LOOP getting better, not just busier?

Contract: the deep audit grades the PRODUCT (QUALITY_SCORECARD); this grades the LOOP.
Update the block below EVERY run in the ONE bookkeeping PR with REAL counts (from git/gh +
this run's record) — honest only (same anti-gaming rule as the business-case number; never
flatter the loop). Dashboard-readable. This is OBSERVABILITY, NOT a ship gate. CLASSIFY
every abandoned change so the loop never re-attempts the same dead-end. A `churning` or
`stuck` signal MUST trigger ONE "loop: harness improvement proposal" issue (the only channel
by which the loop's own rules improve — it cannot edit its routine or `.claude/`).

```yaml
LOOP_HEALTH:
  project: jobscraper
  as_of: 2026-07-12
  last_run: 2026-07-12            # run 43
  last_deep_audit: 2026-07-12    # run 43 (this run) ran the FULL ~8-scout Haiku deep audit (correctness · functional-reality+side-effects · security/abuse · tests-evals+artifact-freshness · design-taste · business-case-lever · store-readiness feasibility · perf/cost+Track-G/H). ~24h since the last full sweep (run 40, 2026-07-11) → cadence-due. run 40 = prior full 8-scout sweep; runs 41/42 were targeted.
  this_run:                      # run 43
    changes_shipped: 3           # #361 billing-path robustness (Stripe sub-budget HTTP timeout on BOTH checkout paths — stripe-py default 80s > Vercel 60s budget — + concurrent add_member race → clean 409, bundled: both share src/org_billing.py) · #362 auth eager-load (joinedload User.subscription at the token-resolution choke-point; removes a lazy +1 for /me + ai-consent + salary-negotiation) · #363 /pricing Team discoverability (surfaces the built-but-unreachable seat tier to net-new/no-account buyers; honest copy, no invented price). (+ this bookkeeping)
    changes_abandoned: 0         # Full 8-scout sweep → selected the MAXIMAL file-disjoint value-bar set (4 findings across 3 PRs). Security scout clean; store-readiness confirmed owner/native/design-gated (all 3 blockers); business-case LIVE-DATA-BLOCKED; perf clean beyond the auth eager-load; 2 design nits skipped as cosmetic churn. No padding, no scarcity.
    abandoned_reasons: []        # reason ∈ gate_tsc|gate_pytest|gate_flake8|gate_lint|gate_build|review_value|review_correctness|circuit_breaker|conflict|dead_end|blocked_owner.
    verify_cycle_failures: 0     # 0 red REQUIRED CI checks at author time. Backend: flake8 clean + targeted suites green locally (billing/org 66, auth 16). Web (#363): tsc --noEmit + eslint clean (npm ci ran locally); existing /pricing e2e asserts only heading+screenshot (intact).
    review_findings_fixed: 4     # maker≠checker earned its keep TWICE on real defects: (A) #361 Reviewer A found a genuine guard-test isolation defect — the process-global stripe.default_http_client singleton made 2/4 timeout tests non-load-bearing in full-file order (they passed even with configure_stripe reverted); FIXED by resetting the global per-test, mutation-re-verified. (B) #362 Reviewer B found a real overclaim — only ~4/39 authed endpoints read user.subscription, not 'every request'; FIXED by rescoping the comment/docstring/PR to the honest narrow scope (kept the shared load site). Plus 2 non-blocking nits fixed: #361 docstring 'cold serverless' wording; #363 'outplacement firms' badge copy.
    review_rejections: 0         # 2 REQUEST_CHANGES (both cycle-1), each actioned + re-reviewed to APPROVE by the SAME reviewer — the process working (a real test-integrity defect + a real honesty overclaim caught pre-merge), NOT abandonments. All other reviewer passes APPROVE first time. 6 code PRs' worth of review = 6 reviewer subagents + 2 re-reviews.
    circuit_breaker_trips: 0
    incidents:                   # self-caught process notes (recovered, NOT abandonments)
      - "SCOUT FALSE-FIX AVERTED (verify-before-act): the functional-reality scout proposed adding timeout= as a kwarg to stripe.checkout.Session.create — but stripe-python 7.9.0's create() has NO timeout kwarg (verified via help()); the SDK's real mechanism is new_default_http_client(timeout=). Implemented the CORRECT mechanism (default is 80s > 60s budget = a real latent outage), not the scout's wrong recipe."
      - "maker≠checker CAUGHT A REAL TEST DEFECT (#361): stripe.default_http_client is a process-global singleton, so 2 of the 4 new timeout tests read ambient global state and would NOT redden a real regression in full-file order (only in isolation) — Reviewer A mutation-proved it. Fixed by resetting the global per test; re-verified the mutation now reddens in normal order. Exactly the guard-test-integrity check maker≠checker exists for."
      - "maker≠checker CAUGHT A REAL OVERCLAIM (#362): the perf comment/body said 'every authenticated request' benefits, but only ~4/39 endpoints read user.subscription — Reviewer B (value/honesty) flagged it. Rescoped to the honest narrow claim (kept the fix; the join is negligible for non-readers). Honesty > a flashy perf story."
      - "GROUND-TRUTH re-probe: no new owner env this run (STRIPE_SECRET_KEY/price IDs still unset → checkout honest-503 paths stay exercised; nightly Career+ RED by design). No external source flipped connected. Store-readiness scout re-confirmed all 3 open FAILs (rendered assets, mobile IAP, native captures) remain genuinely owner/native/design-core — NOT loop-buildable (web@390px screenshots would be store-rejected as non-native)."
    deferred_decisions:          # named + deferred, NOT abandoned (value bar / feasibility / PMF-first — not dead-ends)
      - "login()/register() eager-load (both #362 reviewers named it): login() has the identical lazy user.subscription via user_public, and register() mints a subscription-less user (lazy read → None, not cleanly joinedload-able at construction). Once-per-session paths, lower value than the every-request token choke-point already fixed. Named follow-up, not scarcity."
      - "register postSignupPath backslash open-redirect nuance (#363 Reviewer A, INFORMATIONAL, pre-existing): the ?next guard rejects // but not backslash variants (/\\evil.com) some browsers normalize to protocol-relative. NOT introduced by #363 (which passes a fixed literal). Worth a follow-up hardening ticket; out of scope this run."
      - "2 design-consistency nits (design scout): Team/Settings h1 font-bold vs font-extrabold elsewhere; job-detail subtitle missing mt-1 text-sm. Borderline-cosmetic (§5 lists font-weight/margin tweaks as churn) — SKIPPED to avoid padding; revisit only if bundled into a real surface change."
      - "seat-tier MOBILE admin SURFACE — still native/Linux-unvalidatable (deferred). Business-case floor now LIVE-DATA-BLOCKED: the seat tier is sellable end-to-end on web (backend #348 + /app/team #356 + /pricing discoverability #363) — crossing $100K needs real B2B adoption data, not more building. floor_met_year1 stays false (honest, un-gamed)."
      - "publishing-queue + experiment-engine + ASO/SEO plan + launch-plan doc + §11 media-gen adapter (Track G/H) — SPECULATIVE pre-launch infra (0 users/0 traffic/0 channels). §9 PMF-first: fix the PRODUCT, not acquisition plumbing nothing consumes. Perf/Track-G-H scout independently re-classified as padding. Correctly deferred 42×."
      - "annual-first / founder pricing — annual-first is web/mobile UI (+$2–5K est); founder pricing LOWERS Y1 ARPA + unvalidatable pre-launch (gaming to credit). Named; not this run. mobile IAP client + store assets stay owner/native/design-core (blocked_owner)."
      - "OWNER-BLOCKED (PENDING_OPS, unchanged): the two ship-critical C's — business-case floor ($57.5K<$100K, needs live adoption data) + store-readiness (rendered assets, mobile IAP, native captures) — plus the team/Career+ Stripe price secrets (nightly). NO new external capability this run."
  rolling_7d:
    merged_prs: 135            # repo-wide, approximate (131 through run 42 #356/#357 + run 43 #361/#362/#363); this bookkeeping merges after
    category_mix: {correctness: 2, functional-reality: 1, performance: 1, business-case-strength: 1, tests-evals: 1}   # run 43: #361 (correctness+reliability — Stripe timeout + add_member race) / #362 (performance — auth eager-load) / #363 (business-case-strength — Team discoverability) — 3 disjoint PRs across 4 categories
    diversity: varied
    reverts: 0
    readiness_attempts: 0        # not attempted — the two ship-critical C's stay owner/GTM-blocked (business-case floor needs live adoption data + store assets/IAP/native captures), so submission-readiness is honestly not met
    readiness_rejected: 0
    recurring_failures: []       # the nightly Career+ red is a KNOWN owner-blocked external (missing secret), not a LOOP failure — tracked, not a stuck signal
    harness_proposals_open: 0     # #57 RESOLVED 2026-06-28 — gates now enforced as required checks
  enforced_in_ci: true           # required checks on main (preflight + web E2E), enforce_admins=ON
  auto_migrate_on_deploy: enabled # NO new migration this run. Head unchanged (a7d3e1f0c92b from run 31). Owner must still set Vercel DATABASE_URL→Neon before the next deploy (PENDING_OPS).
  validation:                    # self-validation manifest readiness (compute every run: scripts/check_validation.py --report)
    enforced_in_ci: true         # validate-capabilities runs inside the required `preflight` check
    capabilities_total: 9        # unchanged — no new external capability introduced (#353 is a test-only regression guard).
    unmet: []                    # all external capabilities validated (real or mock)
  eval_coverage:                 # OUTPUT-quality coverage (compute: scripts/check_eval_coverage.py --report)
    enforced_in_ci: true         # check_eval_coverage runs inside the required `preflight` check
    ai_features_total: 5         # unchanged — #353 is entitlement-reconciliation test coverage, not a new LLM feature.
    with_real_output_eval: 5     # each LLM feature judged on REAL Gemini output in CI (tests/evals/test_ai_output_evals.py)
  signal: improving              # bootstrapping | improving | steady | churning | stuck — run 43: 3 shipped (#361 billing-path robustness, #362 auth eager-load, #363 /pricing Team discoverability), 0 abandoned, 0 circuit-breaker trips, 0 red REQUIRED CI checks at author time, 2 REQUEST_CHANGES (both actioned → re-APPROVED), 0 rejections. 'improving' (not steady): this was a FULL 8-scout deep-audit run that selected + shipped the MAXIMAL file-disjoint value-bar set (3 PRs / 4 findings across correctness+reliability, perf, and a monetization surface) — more than the recent 1-change late-convergence runs, driven by real audit findings not padding. maker≠checker earned its keep TWICE on genuine defects caught pre-merge: (1) a guard-test isolation defect that made 2/4 timeout tests non-load-bearing (process-global singleton) — mutation-proven by Reviewer A, fixed + re-verified; (2) a perf-scope OVERCLAIM ('every authenticated request' when only ~4/39 endpoints benefit) — caught by Reviewer B, rescoped honestly. Also averted a scout FALSE-FIX (stripe create() has no timeout kwarg; implemented the real new_default_http_client mechanism instead). The seat tier is now sellable end-to-end on web (backend #348 + /app/team #356 + /pricing discoverability #363); crossing the $100K floor is LIVE-DATA-BLOCKED (real B2B adoption, not building) — floor_met_year1 stays false, un-gamed. Store-readiness re-confirmed owner/native/design-gated. Named follow-ups (login/register eager-load, register backslash-redirect hardening, 2 cosmetic design nits) — no scarcity. No recurring wall → no harness proposal. Prior signals (runs 42/41) retained below.
  # --- run 42 signal (retained) ---
  signal_run42: improving        # run 42: 1 shipped (#356 seat-tier WEB management surface /app/team), 0 abandoned, 0 CB trips, 3 review findings fixed (B's must-fix on a structurally-untested member-mgmt path + 2 nits), 0 rejections. Broke a 3× deferral of the lowest incomplete ROADMAP item by RE-PROBING a stale 'can't validate the web-E2E gate locally' premise (now false). Org tier made user-reachable (was dead code — backend redirected to a /app/team page that didn't exist).
  # --- run 41 signal (retained) ---
  signal_run41: steady           # run 41: 1 shipped (#353 entitlement-reconciliation regression test), 0 abandoned, 0 circuit-breaker trips, 0 red REQUIRED CI checks, 0 review rejections (2/2 clean APPROVE with independent mutation-test proof). A QUIET late-CONVERGENCE run (§2 = SUCCESS): run 40's full 8-scout deep audit (same day) found a clean codebase; run 41 ran a FOCUSED 4-scout sweep of the newest surface and found it clean too. The one value-bar-clearing find was a §26 regression-net gap on a paid-entitlement path → #353.
  # --- run 40 signal (retained) ---
  signal_run40: steady           # run 40: 1 shipped (#351 org-endpoint hardening — concurrent-double-org integrity race + whitespace-name gap), 0 abandoned, 0 circuit-breaker trips, 1 review rejection (Reviewer B dropped a padding email-shape validator, resolved). The full 8-scout deep audit found a CLEAN codebase; the 5th scorecard's ship-critical gaps are RESOLVED on main (functional-reality by runs 36-39; seat-tier backend by #348); a scout false-positive on analytics guards was caught by verify-before-act. No recurring wall → no harness proposal. floor_met_year1 false. FYI prior cadence: run 39 = 1 shipped (improving, the 23×-stuck seat-tier backend), run 38 = 1 (steady), run 37 = 2, run 36 = 2, run 35 = 4.
```
